# Provisional shadow composition

Run `node tools/bake_lighting/shadows/provisional/run.mjs`. This composes opaque
depth and the authenticated native foliage field for the subsequent parity proof.
It is an intermediate, not a runtime publication. See the [shadow parent](../README.md).
