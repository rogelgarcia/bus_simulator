# Prepare spatial probes

`node tools/bake_lighting/diffuse_probes/prepare/run.mjs` exports the current static
city and constructs the virtual diffuse receivers, visibility field and reusable
Blender scene. Uses the shared framework configuration and parent defaults.
See `../README.md` for the complete workflow and publication contract.
