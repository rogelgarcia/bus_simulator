# Prepare lighting baselines

Run `node tools/bake_lighting/experiments/lighting_configurations/prepare/run.mjs`.
This standalone stage validates tracked poses and records installed bake, source
and settings identities in a fresh gitignored run directory. It launches no
browser or Blender. See the [workflow README](../README.md) for inputs and outputs.
