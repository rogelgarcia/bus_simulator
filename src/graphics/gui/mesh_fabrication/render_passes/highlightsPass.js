// src/graphics/gui/mesh_fabrication/render_passes/highlightsPass.js

export function createHighlightsRenderPass() {
    return Object.freeze({
        id: 'highlights',
        render(context) {
            void context;
        }
    });
}
