// src/graphics/gui/mesh_fabrication/render_passes/verticesPass.js

export function createVerticesRenderPass() {
    return Object.freeze({
        id: 'vertices',
        render(context) {
            void context;
        }
    });
}
