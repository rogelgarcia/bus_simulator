# DONE

- Replaced ribbon `Pattern` configuration from picker-popup interaction to inline square pattern blocks in the wall decoration debugger.
- Added side-by-side visual pattern tiles that fit full pattern previews (`object-fit: contain`) with no per-tile labels.
- Added selected pattern name display below the tiles, right-aligned with compact text styling.
